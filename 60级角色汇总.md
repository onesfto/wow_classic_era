# 魔兽世界 60级角色汇总 (哈霍兰)

以下是你名下账号在**哈霍兰**服务器的所有 **60 级** 角色列表：

### 账号: 185418878#1
- 巨龙守护者
- 护士长玛利亚
- 津巴布韦公主
- 魔女桑德兰

### 账号: 216385191#2
- 哈霍兰大格格
- 德加和尚
- 法德迦

### 账号: 601867226#1
- 六扇门沈小梅

### 账号: 601867226#4
- 女王安其拉

---
*注：低于 60 级的角色（如绿龙守护者、暗矛部落酋长等）已根据要求排除，不再记录。*

## 寻找 60 级角色的方法说明

为了找到并筛选出你的满级（60 级）角色，我主要分析了你的魔兽世界插件本地配置存档（WTF 目录），具体步骤如下：

1. **定位关键存档文件**：主要提取了 `163UI_Info.lua`（网易有爱）和 `NovaWorldBuffs.lua`（世界 BUFF 插件）这两个包含了你各个账号所有登陆过角色详细数据的配置档案。
2. **提取等级信息**：
   * 在 `163UI_Info.lua` 中，角色的基础信息是以连续键值对存储的，例如 `["name"] = "巨龙守护者"` 附近会紧跟有 `["level"] = 60`。
   * 在 `NovaWorldBuffs.lua` 的存档字典里，也会详细记录每个角色的 `["level"]` 和 `["playerName"]`。
3. **交叉验证与过滤**：我通过对 `WTF/Account/*/SavedVariables` 目录下的上述文件执行全文检索，寻找各个角色的上下文等级字段。将检索到的所有角色与等级数据进行比对，最终剔除了 1 级、2 级等不满足条件的小号（如：暗矛部落酋长、蓝龙守护者等），只把等级字段明确记录为 60 的角色汇总到了名单中。
